version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/html/
    overwrite: yes
hooks:
  BeforeInstall:
    - location: scripts/BeforeInstall.sh
      timeout: 120
  AfterInstall:
    - location: scripts/AfterInstall.sh
      timeout: 120
